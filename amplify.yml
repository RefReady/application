version: 1
frontend:
  phases:
    build:
      commands:
        - echo "Building RefReady Dashboard..."
        - echo "No build process needed for static site"
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: [] 